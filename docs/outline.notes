
# intro
  what we are doing - explain  a robustlisp
  why ?    - teach lang. design
  how      - w/ scheme and basic haskell
  target audience
  background

# concepts
  lisp evaluation
  interpreter stages
  monads parser
  monad transformers

# Interpreter Structure
  Parse -> Eval -> Result
  text -> environmental eval -> lisp value
  parser -> monad transformer -> data constructor
